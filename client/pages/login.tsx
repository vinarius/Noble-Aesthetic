import React, { ReactElement } from "react";
import { apiClient } from "../api/apiClient";

export default function Login(): ReactElement {
  // apiClient.users.signUp(); // TODO:
  // apiClient.users.login(); // TODO:

  return <>
    <p>This is the login page</p>
  </>;
}